import { kollection } from './kollection.json';

export const Role = {
  Member: kollection.kollectionRoles.Member.code,
  ProjectManager: kollection.kollectionRoles.ProjectManager.code,
  Admin: kollection.kollectionRoles.Admin.code,
};
